// ignore_for_file: file_names
import 'package:flutter/material.dart';

class AnimationGesture extends StatelessWidget {
  const AnimationGesture({Key? key}) : super(key: key);

  @override
  Container build(BuildContext context) {
    return Container();
  }
}
