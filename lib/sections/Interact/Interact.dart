// ignore_for_file: file_names
import 'package:flutter/material.dart';

class Interact extends StatelessWidget {
  const Interact({Key? key}) : super(key: key);

  @override
  Container build(BuildContext context) {
    return Container();
  }
}
